# 프로젝트 개요: 동물상 테스트

이 프로젝트는 Google의 Teachable Machine으로 학습된 이미지 분류 모델을 사용하여 사용자의 얼굴이 강아지상에 가까운지, 고양이상에 가까운지 실시간으로 분석해주는 웹 애플리케이션입니다.

## 기능:
- **실시간 웹캠 분석:** "시작하기" 버튼을 누르면 웹캠이 활성화되고, 실시간으로 사용자의 얼굴을 분석합니다.
- **동물상 예측:** Teachable Machine 모델을 통해 웹캠 이미지를 분석하여 '강아지' 또는 '고양이' 클래스에 대한 확률을 계산합니다.
- **결과 표시:** 각 클래스(강아지, 고양이)에 대한 예측 확률을 실시간으로 화면에 표시하여 사용자에게 피드백을 제공합니다.

## 디자인 및 스타일:
- **레이아웃:** 사용자가 웹캠과 결과에 집중할 수 있도록 간단하고 명료한 중앙 정렬 레이아웃을 사용합니다.
- **색상 팔레트:** `AliceBlue` 배경에 `SteelBlue`와 `CadetBlue`를 포인트 컬러로 사용하여 부드럽고 친근한 느낌을 줍니다.
- **폰트:** 'Cute Font'를 사용하여 귀엽고 재미있는 분위기를 더합니다.
- **컴포넌트:** 웹캠 영상과 결과 레이블이 명확하게 구분되어 보일 수 있도록 깔끔한 컨테이너 스타일을 적용했습니다.

## 구현 계획 (모두 완료):
1.  **`index.html` 재구성:**
    *   Teachable Machine 모델을 실행하는 데 필요한 HTML 구조(제목, 시작 버튼, 웹캠 컨테이너, 레이블 컨테이너)로 페이지를 새로 구성했습니다.
    *   TensorFlow.js 및 Teachable Machine Image 라이브러리를 CDN을 통해 로드합니다.

2.  **`main.js` 로직 변경:**
    *   사용자가 제공한 Teachable Machine 모델 로딩 및 웹캠 설정, 실시간 예측을 위한 JavaScript 코드를 `main.js`로 이전하여 구현했습니다.

3.  **`style.css` 디자인 전면 교체:**
    *   "동물상 테스트" 사이트의 테마에 맞게, 웹캠과 결과 레이블이 명확하게 보일 수 있는 새로운 디자인을 적용했습니다.
